Développé sous Eclipse
--

Pour exécuter proprement le code :
Il faut changer les parametres d'encodage d'eclipse. Preferences > General > Workspace > Text file encoding > ISO-8859-1
Le public static void main se trouve dans BanquePicsou.java

Avancement dans le projet :
Transaction interne faite mais ne fait pas la mise à jour pour un virement differé et ne met pas à jour l'historique... 
(il faudrait mettre un observer dans historique surement)
Transaction externe non faite

Remarque :
Fermer l'application avec la croix ne permet pas d'enregistrer les modifications faites durant la session. C'est un choix arbitraire.

Developped on Eclipse
--

To execute safely the program :
You have to change the Eclipse's encoding parameters. Preferences > General > Workspace > Text file encoding > ISO-8859-1
The public static void main is in BanquePicsou.java (Picsou's bank)

Progression in the project:
Internal transaction made ​​but it does not update for a transfer in differed mode and it does not update the historical ...
(we should surely put an observer)
External transaction not done

Note:
Close the application with the cross does not save the changes made during the session. This is an arbitrary choice .